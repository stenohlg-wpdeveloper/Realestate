---
deployment:
  tasks:
    - export DEPLOYPATH=/home/USERNAME/realestate.bandhudhanmisra.site/
    - rsync -av --exclude='.git' --exclude='.github' --exclude='.cpanel.yml' --exclude='README.md' ./ $DEPLOYPATH
